<UserControl
    x:Class="Signal_Windows.Controls.ThreadView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Signal_Windows.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Visibility="{x:Bind Vm.WelcomeVisibility, Mode=OneWay}" Name="UnselectedBlock">Select a contact and go!</TextBlock>
        <TextBlock Grid.Row="0" Visibility="{x:Bind Vm.MainVisibility, Mode=OneWay}" Name="TitleBlock" Text="{x:Bind Vm.ThreadTitle}" IsTextSelectionEnabled="True" HorizontalAlignment="Center" />
        <ScrollViewer Grid.Row="1" Visibility="{x:Bind  Vm.MainVisibility, Mode=OneWay}" Name="SelectedMessagesScrollViewer" VerticalScrollBarVisibility="Visible" Padding="0 0 14 0">
            <ItemsControl Name="SelectedMessagesList" ItemsSource="{x:Bind Vm.Messages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:MessageBox></local:MessageBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <TextBox Grid.Row="2" Visibility="{x:Bind  Vm.MainVisibility, Mode=OneWay}" VerticalAlignment="Bottom" KeyDown="TextBox_KeyDown"></TextBox>
    </Grid>
</UserControl>