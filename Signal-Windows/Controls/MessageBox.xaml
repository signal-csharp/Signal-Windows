<UserControl x:Name="userControl"
    x:Class="Signal_Windows.Controls.MessageBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Signal_Windows.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="600">

    <Border BorderBrush="#00000000" BorderThickness="1,1,1,1" CornerRadius="4,4,4,4" Background="{x:Bind Background, Mode=TwoWay}" Padding="10 5 10 5" Margin="4">
        <ItemsControl>
            <TextBlock Name="MessageContentTextBlock" TextWrapping="WrapWholeWords" MaxWidth="300" IsTextSelectionEnabled="True" FontSize="14" Foreground="{x:Bind Foreground}" Text="{x:Bind Model.Content}"></TextBlock>
            <ItemsControl Name="MessageAttachments" ItemsSource="{x:Bind Model.Attachments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Name="AttachmentFilenameTextBlock" Text="{Binding FileName}"></TextBlock>
                            <Image Name="AttachmentImage"></Image>
                            <Button Name="AttachmentSaveButton">Save</Button>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ItemsControl>
    </Border>
</UserControl>