<Page
    x:Class="Signal_UWP.Views.AddContactPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Signal_UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding AddContactPageInstance, Source={StaticResource Locator}}">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock>Name</TextBlock>
        <TextBox IsEnabled="{x:Bind Vm.UIEnabled, Mode=OneWay}" Name="NameTextBox" Text="{x:Bind Vm.ContactName, Mode=TwoWay}"></TextBox>
        <TextBlock>Number (e164)</TextBlock>
        <TextBox IsEnabled="{x:Bind Vm.UIEnabled, Mode=OneWay}" Name="NumberTextBox" Text="{x:Bind Vm.ContactNumber, Mode=TwoWay}"></TextBox>
        <Button IsEnabled="{x:Bind Vm.UIEnabled, Mode=OneWay}" Name="CreateButton" Click="CreateButton_Click">Create</Button>
        <Button IsEnabled="{x:Bind Vm.UIEnabled, Mode=OneWay}">Back</Button>
    </StackPanel>
</Page>
